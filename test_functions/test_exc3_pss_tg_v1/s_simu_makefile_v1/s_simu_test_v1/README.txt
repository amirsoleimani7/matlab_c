* NORMAL COMPILE :  

-- in order to compile the program run the command below 

g++ src/*.cpp -Iinclude -Ithird_party/eigen -O2 -std=c++11 -o bin/test_program.exe

-- a exceutable file named test_program.exe will be made in bin directory

cd bin
./test_program

******************************************************

* cmake compile

--  cmake compile will be added soon ...
